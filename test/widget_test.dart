import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('dummy widget test passes', (WidgetTester tester) async {
    // This is just a placeholder widget test so that Assignment 5 runs cleanly.
    final sum = 1 + 1;
    expect(sum, 2);
  });
}
